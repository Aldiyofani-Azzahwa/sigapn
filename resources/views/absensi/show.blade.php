<x-app-layout><div class="rounded-3xl bg-white p-6 shadow dark:bg-slate-900"><h1 class="mb-6 text-2xl font-black">Absensi {{ $kegiatan->nama_kegiatan }}</h1><form method="POST" action="{{ route('absensi.store',$kegiatan) }}" class="mb-6 grid gap-3 md:grid-cols-4">@csrf <select name="anggota_id" class="rounded-2xl border-slate-300 md:col-span-2">@foreach($anggotas as $anggota)<option value="{{ $anggota->id }}">{{ $anggota->nama_lengkap }}</option>@endforeach</select><select name="status" class="rounded-2xl border-slate-300"><option value="hadir">Hadir</option><option value="izin">Izin</option><option value="sakit">Sakit</option><option value="alpha">Alpha</option></select><button class="rounded-2xl bg-emerald-800 font-bold text-white">Simpan</button></form><table class="w-full text-left"><tr><th>Nama</th><th>Status</th><th>Waktu</th></tr>@foreach($kegiatan->absensis as $absensi)<tr class="border-t"><td class="py-3">{{ $absensi->anggota?->nama_lengkap }}</td><td>{{ $absensi->status }}</td><td>{{ $absensi->waktu_absen?->format('d/m/Y H:i') }}</td></tr>@endforeach</table></div></x-app-layout>