<x-app-layout><div class="space-y-6"><h1 class="text-2xl font-black">Absensi Latihan</h1><div class="grid gap-4">@foreach($kegiatans as $kegiatan)<a href="{{ route('absensi.show',$kegiatan) }}" class="rounded-3xl bg-white p-6 shadow dark:bg-slate-900"><h2 class="font-bold">{{ $kegiatan->nama_kegiatan }}</h2><p>{{ $kegiatan->tanggal->format('d/m/Y') }} | {{ $kegiatan->absensis_count }} hadir</p></a>@endforeach</div>{{ $kegiatans->links() }}</div></x-app-layout>